<Window x:Class="GeoArbeitsvorbereitung.Dialogs.LagerBestandDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lagerbestand"
        Width="900" Height="560"
        MinWidth="760" MinHeight="420"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E"
        Foreground="#CCCCCC"
        FontFamily="Segoe UI"
        FontSize="13">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderText"
                   Grid.Row="0"
                   FontSize="15"
                   FontWeight="SemiBold"
                   Margin="0,0,0,6"/>

        <TextBlock x:Name="ExcelText"
                   Grid.Row="1"
                   Foreground="#9E9E9E"
                   FontSize="11"
                   Margin="0,0,0,10"
                   TextWrapping="Wrap"/>

        <DataGrid Grid.Row="2"
                  x:Name="ResultGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Background="#252526"
                  Foreground="#CCCCCC"
                  BorderBrush="#555555"
                  GridLinesVisibility="Horizontal"
                  RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Zeichnungsnummer" Binding="{Binding Zeichnungsnummer}" Width="2*"/>
                <DataGridTextColumn Header="Material" Binding="{Binding Material}" Width="*"/>
                <DataGridTextColumn Header="Bestand" Binding="{Binding Bestand}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3"
                Content="Schließen"
                HorizontalAlignment="Right"
                Margin="0,10,0,0"
                Padding="16,7"
                Click="Close_Click"/>
    </Grid>
</Window>
