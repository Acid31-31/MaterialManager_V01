<Window x:Class="MaterialManager_V01.Views.ResteSucheDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Material suchen" Height="700" Width="1400" Background="{DynamicResource ThemeWindowBackgroundBrush}" Foreground="{DynamicResource ThemeForegroundBrush}" WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
        </Style>

        <Style x:Key="SearchLabelTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
            <Setter Property="Margin" Value="0,8,0,4" />
        </Style>

        <Style x:Key="SearchComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{DynamicResource ThemeSurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Opacity" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Name="Border"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ThemeAltSurfaceBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ThemeAltSurfaceBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
                                <Setter Property="Opacity" Value="1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem" BasedOn="{StaticResource SearchComboBoxItemStyle}" />

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource ThemeSurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderBrush}" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Height" Value="32" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource ThemeSurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderBrush}" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"/>

                            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>

                            <ToggleButton Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Opacity="0.01"/>

                            <Popup x:Name="PART_Popup"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="{DynamicResource ThemeSurfaceBrush}"
                                        BorderBrush="{DynamicResource ThemeBorderBrush}"
                                        BorderThickness="1"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#1976D2" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="Margin" Value="8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Erweiterte Material-Suche" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" Text="Form / Typ:" Style="{StaticResource SearchLabelTextStyle}"/>
        <ComboBox Grid.Row="1" x:Name="FormBox" Margin="0,32,0,0" SelectedIndex="0" ItemContainerStyle="{StaticResource SearchComboBoxItemStyle}">
            <ComboBoxItem>Alle</ComboBoxItem>
            <ComboBoxItem>Rest</ComboBoxItem>
            <ComboBoxItem>GF</ComboBoxItem>
            <ComboBoxItem>MF</ComboBoxItem>
            <ComboBoxItem>KF</ComboBoxItem>
            <ComboBoxItem>Rundrohr</ComboBoxItem>
            <ComboBoxItem>Vierkantrohr</ComboBoxItem>
            <ComboBoxItem>Rechteckrohr</ComboBoxItem>
        </ComboBox>

        <TextBlock Grid.Row="2" Text="Material:" Style="{StaticResource SearchLabelTextStyle}"/>
        <ComboBox Grid.Row="2" x:Name="MaterialBox" Margin="0,32,0,0" SelectedIndex="0" SelectionChanged="MaterialBox_SelectionChanged" ItemContainerStyle="{StaticResource SearchComboBoxItemStyle}">
            <ComboBoxItem>Alle</ComboBoxItem>
            <ComboBoxItem>Stahl</ComboBoxItem>
            <ComboBoxItem>Edelstahl</ComboBoxItem>
            <ComboBoxItem>Aluminium</ComboBoxItem>
        </ComboBox>

        <TextBlock Grid.Row="3" Text="Legierung:" Style="{StaticResource SearchLabelTextStyle}"/>
        <ComboBox Grid.Row="3" x:Name="LegierungBox" Margin="0,32,0,0"
                  ItemsSource="{Binding Legierungen}"
                  ItemContainerStyle="{StaticResource SearchComboBoxItemStyle}"
                  SelectedIndex="0">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Foreground="{DynamicResource ThemeForegroundBrush}" Margin="0"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Grid.Row="4" Text="Stärke (mm):" Style="{StaticResource SearchLabelTextStyle}"/>
        <ComboBox Grid.Row="4" x:Name="StaerkeBox" Margin="0,32,0,0" SelectedIndex="0" ItemContainerStyle="{StaticResource SearchComboBoxItemStyle}">
            <ComboBoxItem>Alle</ComboBoxItem>
            <ComboBoxItem>0,3</ComboBoxItem>
            <ComboBoxItem>0,5</ComboBoxItem>
            <ComboBoxItem>0,8</ComboBoxItem>
            <ComboBoxItem>1,0</ComboBoxItem>
            <ComboBoxItem>1,5</ComboBoxItem>
            <ComboBoxItem>2,0</ComboBoxItem>
            <ComboBoxItem>3,0</ComboBoxItem>
            <ComboBoxItem>4,0</ComboBoxItem>
            <ComboBoxItem>5,0</ComboBoxItem>
            <ComboBoxItem>6,0</ComboBoxItem>
            <ComboBoxItem>8,0</ComboBoxItem>
            <ComboBoxItem>10,0</ComboBoxItem>
            <ComboBoxItem>12,0</ComboBoxItem>
            <ComboBoxItem>15,0</ComboBoxItem>
            <ComboBoxItem>20,0</ComboBoxItem>
            <ComboBoxItem>25,0</ComboBoxItem>
        </ComboBox>

        <TextBlock Grid.Row="5" Text="Maße (optional, z.B. 1000x550):" Style="{StaticResource SearchLabelTextStyle}"/>
        <TextBox Grid.Row="5" x:Name="MassBox" Margin="0,32,0,0" 
                 ToolTip="Format: LängexBreite (in mm)&#x0a;Zuerst werden exakte Maßtreffer gesucht.&#x0a;Wenn kein exakter Rest vorhanden ist, werden größere Reste angezeigt.&#x0a;Wenn keine Reste passen, werden passende Tafeln angezeigt."/>

        <TextBlock Grid.Row="6" Text="Toleranz (%):" Style="{StaticResource SearchLabelTextStyle}" Visibility="Collapsed"/>
        <ComboBox Grid.Row="6" x:Name="ToleranzBox" Margin="0,32,0,0" SelectedIndex="1" Visibility="Collapsed" ItemContainerStyle="{StaticResource SearchComboBoxItemStyle}">
            <ComboBoxItem>5%</ComboBoxItem>
            <ComboBoxItem>10%</ComboBoxItem>
            <ComboBoxItem>15%</ComboBoxItem>
            <ComboBoxItem>20%</ComboBoxItem>
            <ComboBoxItem>25%</ComboBoxItem>
            <ComboBoxItem>30%</ComboBoxItem>
        </ComboBox>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Suchen" Click="OnSuchen" Width="120"/>
            <Button Content="Abbrechen" Click="OnAbbrechen" Width="120" Background="#555"/>
        </StackPanel>
    </Grid>
</Window>
