<Window x:Class="MaterialManager_V01.Views.ResteSucheDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🔍 Reste suchen" Height="700" Width="1400" Background="#0B0B0B" WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="0,8,0,4" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#222" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#444" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Height" Value="32" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#222" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#444" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton
                                Name="ToggleButton"
                                Grid.Column="2"
                                Focusable="false"
                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                ClickMode="Press"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            </ToggleButton>
                            <ContentPresenter
                                Name="ContentSite"
                                IsHitTestVisible="False"
                                Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                Margin="3,3,23,3"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left">
                            </ContentPresenter>
                            <TextBox
                                x:Name="PART_EditableTextBox"
                                Visibility="Hidden"
                                IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup
                                Name="Popup"
                                Placement="Bottom"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True"
                                Focusable="False"
                                PopupAnimation="Slide">
                                <Grid
                                    Name="DropDown"
                                    SnapsToDevicePixels="True"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border
                                        x:Name="DropDownBorder"
                                        Background="#222"
                                        BorderThickness="1"
                                        BorderBrush="#444">
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#222" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border
                            Name="Border"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#4CAF50" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#333" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#1976D2" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="Margin" Value="8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Erweiterte Reste-Suche" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" Text="Material:"/>
        <ComboBox Grid.Row="1" x:Name="MaterialBox" Margin="0,32,0,0" SelectedIndex="0" SelectionChanged="MaterialBox_SelectionChanged">
            <ComboBoxItem>Alle</ComboBoxItem>
            <ComboBoxItem>Stahl</ComboBoxItem>
            <ComboBoxItem>Edelstahl</ComboBoxItem>
            <ComboBoxItem>Aluminium</ComboBoxItem>
        </ComboBox>

        <TextBlock Grid.Row="2" Text="Legierung:"/>
        <ComboBox Grid.Row="2" x:Name="LegierungBox" Margin="0,32,0,0" 
                  ItemsSource="{Binding Legierungen}"
                  SelectedIndex="0"/>

        <TextBlock Grid.Row="3" Text="Stärke (mm):"/>
        <ComboBox Grid.Row="3" x:Name="StaerkeBox" Margin="0,32,0,0" SelectedIndex="0">
            <ComboBoxItem>Alle</ComboBoxItem>
            <ComboBoxItem>0,3</ComboBoxItem>
            <ComboBoxItem>0,5</ComboBoxItem>
            <ComboBoxItem>0,8</ComboBoxItem>
            <ComboBoxItem>1,0</ComboBoxItem>
            <ComboBoxItem>1,5</ComboBoxItem>
            <ComboBoxItem>2,0</ComboBoxItem>
            <ComboBoxItem>3,0</ComboBoxItem>
            <ComboBoxItem>4,0</ComboBoxItem>
            <ComboBoxItem>5,0</ComboBoxItem>
            <ComboBoxItem>6,0</ComboBoxItem>
            <ComboBoxItem>8,0</ComboBoxItem>
            <ComboBoxItem>10,0</ComboBoxItem>
            <ComboBoxItem>12,0</ComboBoxItem>
            <ComboBoxItem>15,0</ComboBoxItem>
            <ComboBoxItem>20,0</ComboBoxItem>
            <ComboBoxItem>25,0</ComboBoxItem>
        </ComboBox>

        <TextBlock Grid.Row="4" Text="Ungefähre Maße (z.B. 1000x550):"/>
        <TextBox Grid.Row="4" x:Name="MassBox" Margin="0,32,0,0" 
                 ToolTip="Format: LängexBreite (in mm)&#x0a;Es werden ähnliche Maße mit ±10% Toleranz gefunden"/>

        <TextBlock Grid.Row="5" Text="Toleranz (%):"/>
        <ComboBox Grid.Row="5" x:Name="ToleranzBox" Margin="0,32,0,0" SelectedIndex="4">
            <ComboBoxItem>5%</ComboBoxItem>
            <ComboBoxItem>10%</ComboBoxItem>
            <ComboBoxItem>15%</ComboBoxItem>
            <ComboBoxItem>20%</ComboBoxItem>
            <ComboBoxItem>50%</ComboBoxItem>
            <ComboBoxItem>75%</ComboBoxItem>
            <ComboBoxItem>100%</ComboBoxItem>
            <ComboBoxItem>150%</ComboBoxItem>
            <ComboBoxItem>200%</ComboBoxItem>
        </ComboBox>

        <TextBlock Grid.Row="6" Text="Form:"/>
        <ComboBox Grid.Row="6" x:Name="FormBox" Margin="0,32,0,0" SelectedIndex="0">
            <ComboBoxItem>Alle</ComboBoxItem>
            <ComboBoxItem>Rest</ComboBoxItem>
            <ComboBoxItem>GF</ComboBoxItem>
            <ComboBoxItem>MF</ComboBoxItem>
            <ComboBoxItem>KF</ComboBoxItem>
        </ComboBox>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Suchen" Click="OnSuchen" Width="120"/>
            <Button Content="Abbrechen" Click="OnAbbrechen" Width="120" Background="#555"/>
        </StackPanel>
    </Grid>
</Window>
