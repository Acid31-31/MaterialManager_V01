<Window x:Class="MaterialManager_V01.Views.NetzwerkEinstellungenDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Netzwerk‑Einstellungen" Height="320" Width="520" Background="#0B0B0B" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="0,4" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#222" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#444" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Height" Value="30" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1976D2" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="6" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="0,6" />
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Netzwerk‑Modus" FontSize="16" FontWeight="Bold" Grid.Row="0" />
        <CheckBox x:Name="AktivCheck" Grid.Row="1" Content="Netzwerk‑Modus aktivieren" />

        <TextBlock Text="Netzwerkpfad:" Grid.Row="2" />
        <TextBox x:Name="PfadBox" Grid.Row="3" />

        <TextBlock Text="Benutzername (optional):" Grid.Row="4" />
        <TextBox x:Name="BenutzerBox" Grid.Row="5" />

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Speichern" Click="OnSave" Width="100" />
            <Button Content="Abbrechen" Click="OnCancel" Width="100" Background="#555" />
        </StackPanel>
    </Grid>
</Window>
