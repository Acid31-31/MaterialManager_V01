<Window x:Class="MaterialManager_V01.Views.NetzwerkEinstellungenDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Netzwerk-Einstellungen" Height="520" Width="620" Background="#0B0B0B" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="0,4" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#222" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#444" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Height" Value="30" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1976D2" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="6" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="0,6" />
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Netzwerk-Modus" FontSize="16" FontWeight="Bold" Grid.Row="0" />
        <CheckBox x:Name="AktivCheck" Grid.Row="1" Content="Netzwerk-Modus aktivieren" />

        <TextBlock Text="Gemeinsamer Netzwerkpfad (Materialdaten):" Grid.Row="2" />
        <TextBox x:Name="PfadBox" Grid.Row="3" />

        <TextBlock Text="Gemeinsamer Auftrags-Archivpfad (Laser/Tafelplanung):" Grid.Row="4" Margin="0,10,0,4"/>
        <TextBox x:Name="ArchivPfadBox" Grid.Row="5" />

        <TextBlock Grid.Row="6" Foreground="#A0A0A0" TextWrapping="Wrap" Margin="0,4,0,0"
                   Text="Pfad muss auf allen Rechnern identisch sein (z.B. \\SERVER\MaterialManager\Auftragsarchiv oder Z:\Auftragsarchiv)."/>

        <TextBlock Text="Benutzername (optional):" Grid.Row="7" Margin="0,10,0,4" />
        <TextBox x:Name="BenutzerBox" Grid.Row="8" />

        <Border Grid.Row="10" Grid.RowSpan="2" Background="#1A1A1A" BorderBrush="#2E2E2E" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,10,0,0">
            <TextBlock TextWrapping="Wrap" Foreground="#D0D0D0">
                So erstellen Sie den gemeinsamen Archivordner:
                &#x0a;1) Auf Server/NAS einen Ordner anlegen (z.B. Auftragsarchiv).
                &#x0a;2) Freigabe aktivieren und allen beteiligten Rechnern Lese-/Schreibrechte geben.
                &#x0a;3) Auf jedem PC denselben UNC-Pfad oder denselben Laufwerksbuchstaben verwenden.
                &#x0a;4) Hier exakt denselben Pfad auf allen PCs eintragen.
            </TextBlock>
        </Border>

        <StackPanel Grid.Row="12" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Speichern" Click="OnSave" Width="100" />
            <Button Content="Abbrechen" Click="OnCancel" Width="100" Background="#555" />
        </StackPanel>
    </Grid>
</Window>
