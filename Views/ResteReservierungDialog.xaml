<Window x:Class="MaterialManager_V01.Views.ResteReservierungDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rest reservieren" Height="420" Width="520" Background="#0B0B0B" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="0,4" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#222" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#555" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Height" Value="32" />
        </Style>
        <!-- Sichtbare Checkbox für dunkles Design -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border x:Name="CheckBorder" Width="18" Height="18"
                                    Background="#2A2A2A" BorderBrush="#777" BorderThickness="1.5"
                                    CornerRadius="2" Margin="0,0,8,0" VerticalAlignment="Center">
                                <TextBlock x:Name="CheckMark" Text="✓" Foreground="#4CAF50"
                                           FontWeight="Bold" FontSize="13"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#4CAF50"/>
                                <Setter TargetName="CheckBorder" Property="Background" Value="#1A3A1A"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#AAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1976D2" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="6" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Material reservieren" FontSize="16" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,10"/>

        <TextBlock Text="Auftragsnummer / Eintrag:" Grid.Row="1"/>
        <TextBox x:Name="AuftragBox" Grid.Row="2"/>

        <!-- Stückzahl-Bereich (nur sichtbar wenn Material > 1 Stück hat) -->
        <StackPanel Grid.Row="3" x:Name="StueckzahlPanel" Visibility="Collapsed" Margin="0,10,0,0">
            <Border Background="#1A2A1A" BorderBrush="#4CAF50" BorderThickness="1" CornerRadius="4" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="VerfuegbarText" Text="Verfügbar: 0 Stück" Foreground="#4CAF50" FontWeight="Bold"/>
                        <TextBlock Text="Benötigte Stückzahl:" Foreground="#CCC" Margin="0,6,0,0"/>
                        <TextBox x:Name="StueckzahlBox" Width="100" HorizontalAlignment="Left"
                                 Background="#222" Foreground="White" BorderBrush="#4CAF50"
                                 Height="32" Padding="8,4" Margin="0,4,0,0"
                                 PreviewTextInput="OnStueckzahlPreviewInput"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                        <TextBlock x:Name="RestText" Text="→ 0 Stück\nbleiben im Lager"
                                   Foreground="#888" TextAlignment="Center" TextWrapping="Wrap"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0,6,0,0">
            <CheckBox x:Name="NachproduktionCheck" Content="Für Nachproduktion reservieren (ohne Auftragsnummer)"
                      Checked="OnNachproduktionChecked" Unchecked="OnNachproduktionUnchecked"/>
            <CheckBox x:Name="DeleteMaterialCheck" Content="Material direkt aus dem Lager löschen"/>
        </StackPanel>

        <TextBlock Grid.Row="5" Margin="0,10,0,0"
                   Text="Leer lassen = Reservierung entfernen. Nachproduktion setzt automatisch den Eintrag 'Nachproduktion'."
                   Foreground="#777" TextWrapping="Wrap" FontSize="11"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="OK" Click="OnOk" Width="80"/>
            <Button Content="Abbrechen" Click="OnCancel" Width="100" Style="{StaticResource AppButton3DSecondaryStyle}"/>
        </StackPanel>
    </Grid>
</Window>
