<Window x:Class="MaterialManager_V01.Views.ResteReservierungDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rest reservieren" Height="300" Width="520" Background="#0B0B0B" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="0,4" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#222" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#444" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Height" Value="30" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1976D2" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="6" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Material reservieren" FontSize="16" FontWeight="Bold" Grid.Row="0" />
        <TextBlock Text="Auftragsnummer / Eintrag:" Grid.Row="1" />
        <TextBox x:Name="AuftragBox" Grid.Row="2" />

        <TextBlock Grid.Row="3" Text="Leer lassen = Reservierung entfernen. Mit 'Eintrag löschen' wird Material komplett aus dem Lager gelöscht." Foreground="#888" TextWrapping="Wrap" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Eintrag löschen" Click="OnDeleteEntry" Width="130" Background="#C62828" />
            <Button Content="OK" Click="OnOk" Width="80" />
            <Button Content="Abbrechen" Click="OnCancel" Width="100" Background="#555" />
        </StackPanel>
    </Grid>
</Window>
