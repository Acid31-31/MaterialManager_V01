<Window x:Class="MaterialManager_V01.Views.EuPaletteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EU Palette (Rest Material)" Height="600" Width="1200" Background="#0B0B0B" WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="0,4" />
        </Style>

        <Style x:Key="FilterComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#222" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#444" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton
                                Name="ToggleButton"
                                Grid.Column="2"
                                Focusable="false"
                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                ClickMode="Press"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            </ToggleButton>
                            <ContentPresenter
                                Name="ContentSite"
                                IsHitTestVisible="False"
                                Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                Margin="3,3,23,3"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left">
                            </ContentPresenter>
                            <TextBox
                                x:Name="PART_EditableTextBox"
                                Visibility="Hidden"
                                IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup
                                Name="Popup"
                                Placement="Bottom"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True"
                                Focusable="False"
                                PopupAnimation="Slide">
                                <Grid
                                    Name="DropDown"
                                    SnapsToDevicePixels="True"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border
                                        x:Name="DropDownBorder"
                                        Background="#222"
                                        BorderThickness="1"
                                        BorderBrush="#444">
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#222" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border
                            Name="Border"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1976D2" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#333" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#1976D2" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="Margin" Value="8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="RowBackground" Value="#151515" />
            <Setter Property="AlternatingRowBackground" Value="#121212" />
            <Setter Property="GridLinesVisibility" Value="None" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="BorderBrush" Value="#222" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#121212" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#151515" />
            <Setter Property="Foreground" Value="White" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#252525" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1E1E1E" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="BorderBrush" Value="#333" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="📦 EU Palette (Rest Material)" FontSize="18" FontWeight="Bold"/>
            <TextBlock Text="Kleine Materialien (≤1500x1300 oder ≤1300x1500)" FontSize="12" Foreground="#888" Margin="0,4,0,8"/>
        </StackPanel>

        <!-- Statistik -->
        <Border Grid.Row="1" Background="#1E1E1E" Padding="12" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="{Binding GesamtStatistik}" FontSize="14" FontWeight="Bold" Foreground="#4CAF50"/>
                <TextBlock Text="{Binding AuslastungInfo}" FontSize="12" Foreground="#888" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Material-Filter -->
        <Border Grid.Row="2" Background="#1E1E1E" Padding="12" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="🔍 Filter-Optionen" FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>

                <!-- Erste Zeile: Material, Legierung, Oberfläche -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Material:" FontSize="11" Foreground="#888" Margin="0,0,0,4"/>
                        <ComboBox x:Name="MaterialFilterBox" Style="{StaticResource FilterComboBox}" Width="150" SelectionChanged="OnMaterialSelectionChanged">
                            <ComboBoxItem IsSelected="True">Alle</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Legierung:" FontSize="11" Foreground="#888" Margin="0,0,0,4"/>
                        <ComboBox x:Name="LegierungFilterBox" Style="{StaticResource FilterComboBox}" Width="150" SelectionChanged="OnFilterChanged">
                            <ComboBoxItem IsSelected="True">Alle</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Oberfläche:" FontSize="11" Foreground="#888" Margin="0,0,0,4"/>
                        <ComboBox x:Name="OberflaecheFilterBox" Style="{StaticResource FilterComboBox}" Width="150" SelectionChanged="OnFilterChanged">
                            <ComboBoxItem IsSelected="True">Alle</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Güte:" FontSize="11" Foreground="#888" Margin="0,0,0,4"/>
                        <ComboBox x:Name="GueteFilterBox" Style="{StaticResource FilterComboBox}" Width="120" SelectionChanged="OnFilterChanged">
                            <ComboBoxItem IsSelected="True">Alle</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>
                </StackPanel>

                <!-- Zweite Zeile: Stärke, Statistik -->
                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Stärke:" FontSize="11" Foreground="#888" Margin="0,0,0,4"/>
                        <ComboBox x:Name="StaerkeFilterBox" Style="{StaticResource FilterComboBox}" Width="120" SelectionChanged="OnFilterChanged">
                            <ComboBoxItem IsSelected="True">Alle</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel VerticalAlignment="Bottom" Margin="0,0,0,4">
                        <TextBlock Text="{Binding FilterStatistik}" FontWeight="Bold" Foreground="#FFC107" FontSize="13"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <DataGrid Grid.Row="3" x:Name="PaletteGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  MouseDoubleClick="OnMaterialDoppelklick"
                  RowHeight="36">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Material" Binding="{Binding MaterialArt}" Width="120"/>
                <DataGridTextColumn Header="Legierung" Binding="{Binding Legierung}" Width="120"/>
                <DataGridTextColumn Header="Oberfläche" Binding="{Binding Oberflaeche}" Width="160"/>
                <DataGridTextColumn Header="Güte" Binding="{Binding Guete}" Width="90"/>
                <DataGridTextColumn Header="Stärke" Binding="{Binding Staerke, StringFormat={}{0:0.0} mm}" Width="90"/>
                <DataGridTextColumn Header="Maß" Binding="{Binding Mass}" Width="140"/>
                <DataGridTextColumn Header="Stück" Binding="{Binding Stueckzahl}" Width="60"/>
                <DataGridTextColumn Header="Gewicht (kg)" Binding="{Binding GewichtKg, StringFormat={}{0:0.00}}" Width="100"/>
                <DataGridTextColumn Header="Lagerort" Binding="{Binding Lagerort}" Width="110"/>
                <DataGridTextColumn Header="Angelegt von" Binding="{Binding AngelegtVon}" Width="110"/>
                <DataGridTextColumn Header="Geändert von" Binding="{Binding GeaendertVon}" Width="110"/>
                <DataGridTextColumn Header="Geändert" Binding="{Binding AenderungsDatum, StringFormat=dd.MM.yyyy}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Schließen" Click="OnSchliessen" Width="120" Background="#555"/>
        </StackPanel>
    </Grid>
</Window>
